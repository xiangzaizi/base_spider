
# Automatically created by: scrapyd-deploy

from setuptools import setup, find_packages

setup(
    name         = 'project',
    version      = '1.0',
    packages     = find_packages(),
    entry_points = {'scrapy': ['settings = Douyu.settings']},
)

if __name__ == '__main__':
	def send_request()
	url = url
	headers = {"User-Agent"}
	request = urllib2.Request(url, headers=headers)
	html = urllib2.urlopen(request).read()  # 简单的爬下来整个页面
	print html 

send_request



